/**
 * @file constants/layout.js
 * @description JS 런타임에서 참조하는 공통 레이아웃 기준값입니다.
 */

export const LAYOUT_WIDTH = Object.freeze({
  prompt: 880,
  message: 880,
  chatLane: 880,
  minCodePanelChat: 420,
});
