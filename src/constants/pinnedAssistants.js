/**
 * Sidebar default assistants (collapsed view).
 * Only these assistants are shown until the user clicks "더보기".
 * If an id doesn't exist in data.json, it will be ignored.
 */
export const PINNED_ASSISTANT_IDS = ["ds", "spec"];
